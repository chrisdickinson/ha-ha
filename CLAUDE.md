# ha-ha

Boundary nomination: name interfaces the compiler can't see as one unit, track how
they change, and check changes against deterministic + LLM-judged rules.

## Project memory

This project uses the trivia MCP. All memories are tagged `project:ha-ha`. Recall by
that tag at the start of work (`recall(query, tags=["project:ha-ha"])`). Add new
lessons via the `session-retro` skill.
